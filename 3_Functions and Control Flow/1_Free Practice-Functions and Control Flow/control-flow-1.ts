const area10: number = Math.PI * 10 * 10;
const area5: number = Math.PI * 5 * 5;
const areaAnnulus: number = area10 - area5;

const str: string = "If the annulus has an outer circle with area " + area10 + " and inner circle with area " + area5 + ", its area is " + areaAnnulus;