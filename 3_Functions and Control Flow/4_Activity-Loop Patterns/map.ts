// Map
const someNums: number[] = [
  10,
  22,
  -3,
  5
];

// Get a list of all the names but uppercase
const squared: number[] = [];

for (const current of someNums) {
  squared.push(current * current);
}
